name: flask deployemnt
on: 
  push:
    branches: [ main ]

jobs:
  Deploy:
    name: deploy to EC2
    runs-on: ubuntu-latest

  steps:
  -name: Deploy in EC2
  env:
      PRIVATE_KEY: ${{ secrets.SSH_KEY }}
      HOST_KEY: ${{ secrets.HOST_NAME }}
      USER_NAME: ${{ secrets.USER_NAME }}
